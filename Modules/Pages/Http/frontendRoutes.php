<?php

use Illuminate\Support\Facades\Route;

/*if ($page = \MyApp::getPageLinkedToModule('schedules')) {
    $options = [];
    if ($uri = $page->uri) {
        Route::get($uri, $options + ['as' => 'schedules', 'uses' => 'SchedulesPublicController@index']);
        Route::get($uri.'/{slug}', $options +  ['as' => 'schedules.slug', 'uses' => 'SchedulesPublicController@show']);
    }
}*/

/*Route::group(['prefix' => 'schedules'], function()
{
    Route::get('/', 'SchedulesPublicController@index');
});*/
